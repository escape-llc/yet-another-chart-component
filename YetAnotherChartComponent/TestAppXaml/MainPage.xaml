<Page
    x:Class="TestAppXaml.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestAppXaml"
		xmlns:charts="using:eScapeLLC.UWP.Charts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock HorizontalAlignment="Stretch" TextAlignment="Center" Text="eScape LLC Chart Demo"/>
		<charts:Chart Grid.Row="1" Background="Beige" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
			<charts:Chart.Components>
				<charts:CategoryAxis x:Name="xaxis" />
				<charts:ValueAxis Name="yaxis" />
				<charts:LineSeries DataSource="{Binding Data}" ValueAxis="{Binding ElementName=yaxis}" />
			</charts:Chart.Components>
		</charts:Chart>
	</Grid>
</Page>
